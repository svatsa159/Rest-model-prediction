_f73‍.x([["schema",()=>schema]]);const { makeExecutableSchema } = require('graphql-tools')
const { GraphQLUpload } = require('graphql-upload')

const typeDefs = `
  scalar Upload
`

const resolvers = {
  Upload: GraphQLUpload
}

       const schema = makeExecutableSchema({ typeDefs, resolvers })
